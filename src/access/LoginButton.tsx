import React from 'react';
import { Button } from '@mui/material';

export default function LoginButton() {
  return (
    <Button
      style={{ width: '100%' }}
      variant="outlined"
    >
      Log in
    </Button>
  );
}
